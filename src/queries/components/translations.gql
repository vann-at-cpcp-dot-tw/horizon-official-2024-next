query QueryTranslations(
  $language: LanguageCodeFilterEnum = EN,
) {
  translationSettings(language: $language) {
    translationFields {
      translations {
        key
        value
      }
    }
  }
}